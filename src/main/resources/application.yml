graphql:
  packages:
    - "com.workshop.graphql.bff"
  sdl:
    enabled: true
spring:
  application:
    name: WS-GRAPHQL-BFF

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

adapter:
  sidecar-egress: ${SIDECAR_EGRESS_URL}