graphql:
  packages:
    - "com.workshop.graphql.bff"
  sdl:
    enabled: true
spring:
  application:
    name: WS-GRAPHQL-BFF

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

adapter:
  user-service-url: ${USER_SERVICE_URL}
  api-key: ${API_KEY}