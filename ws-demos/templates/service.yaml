apiVersion: v1
kind: Service
metadata:
  name: {{ include "ws-demos.fullname" . }}
  labels:
    {{- include "ws-demos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ws-demos.selectorLabels" . | nindent 4 }}
